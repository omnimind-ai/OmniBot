# Remove fixed four-call parallel tool cap

Request: remove the fixed four-tool limit and explain implementation/competency level. Changed only contiguous parallelSafe grouping in AgentOrchestrator: removed take(4). Writes, GUI, terminal and unclassified tools remain serial barriers. Results still commit in declared order; cancellation remains structured. No system prompt, provider selection, ACP lifecycle, or subagent semaphore was changed. This removes a scheduler batch-size cap, not operating-system/provider/resource limits.

Executable regression: AgentOrchestratorTest.independentReadsOverlapButWritesRemainBarriersAndHistoryKeepsModelOrder failed on old code with expected9 actual4. Updated existing cancellation regression to wait for all8 starts before cancellation, and 12-read benchmark to expect peak12. After the one-line implementation change, all71 AgentOrchestratorTest cases pass. Release build succeeds. Controlled benchmark615ms serial vs54ms parallel for12 simulated50ms waits is not end-to-end Agent speedup.

Physical PJE110 b49f281b: installed productionStandardRelease0.6.3/code16 using existing test certificate at2026-09-18 09:20:48. APK db92c4b6e5eed95e4e830e60007c669b5baa864f1f9482fc61f2319feda96063. First real UI request in conversation89 showed execution failure before tools; old conversation already showed an empty-assistant/DONE error and had102658/128000 tokens. A fresh conversation retry was attempted with the same model and prompt configuration. One UI sender failed before sending due uiautomator137 after the prior observer was stopped; the following attempt sent successfully. No content-free AgentPerformance records were captured from this Release attempt, so actual overlap cannot be asserted. **待真机验证：超过4个工具同时执行尚未通过验收。** Do not treat installation or JVM tests as physical concurrency acceptance.

Persistent real-model fixture: scripts/fixtures/agent-user-journeys/agent-eight-safe-reads.json with eight direct file_search calls against the existing recording-recovery-demo folder. Uses existing UI runner; does not inject fake model/tool results. scripts/assert-agent-parallel-timings.py accepts the actual runtime agentRunId from captured AgentPerformance records and requires eight unique returned calls in one round, overlap greater than4 and a completed turn. A prompt marker is NOT the agentRunId; if no records are captured, runtime verification is blocked rather than substituting an id. Timing assertion alone does not prove result correctness.

Run unit tests with Android Studio JBR: ./gradlew --no-daemon --no-parallel :app:testDevelopStandardDebugUnitTest --tests '*AgentOrchestratorTest'. Physical fixture: OOB_ALLOW_PHYSICAL_DEVICE=1 node scripts/verify-agent-user-journey.mjs b49f281b scripts/fixtures/agent-user-journeys/agent-eight-safe-reads.json /tmp/eight-reads. Inspect actual tool outputs and timing identities before claiming acceptance.

Final fresh-conversation physical result: send succeeded, but no expected assistant completion appeared within240 seconds; zero unavailable UI snapshots. Runner exited1. Preserved physical-ui-result.json. Device acceptance failed/pending; no actual eight-tool concurrency is claimed. Diagnostic log capture stopped afterward.
