{
  "prompt": "Real UI acceptance FAILED: water check-in raises NOT NULL check_ins.day; XP remains 0. Fix life-xp-demo then republish preserving data. Actual SQLite connector supports config.table only, NOT config.sql/values/conflict. Query returns all table rows; join habits and check_ins and sum XP in JavaScript, do not assume get_stats returns SQL aggregates. Declare day string in check_in_habit required parameters, pass LOCAL calendar date from JS. Load saved check_ins to disable today's completed buttons, disable in-flight too. Add visible level-up animation/toast when crossing 50 XP and historical level milestones derived from saved events. Escape user habit text via textContent. Keep a visible friendly avatar on main screen. History must show event date, habit, XP and daily total. Read current source/contract and fix all affected files, project_check and publish. No mock data; no new app. Perform work now."
}