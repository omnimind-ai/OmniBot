{"prompt": "Actual UI: 3 check-ins = 60 XP and level 2 works, duplicates blocked. History WRONG: each XP and daily total show zero; no level milestone. Fix renderHistory: map habit id to full habit object, use habit.xp_value (check_ins has no xp_value). Compute chronological cumulative XP and render each crossed 50-XP level milestone. Fix UTC day to local YYYY-MM-DD. Preserve the 3 existing records. Make small targeted edits, check and republish life-xp-demo now."}