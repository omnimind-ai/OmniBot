interface:
  display_name: "Install Codex Pet"
  short_description: "Install shared Codex pets into Omnibot"
  default_prompt: "Use $install-codex-pet to handle a shared pet request without running an unaudited network installer."
